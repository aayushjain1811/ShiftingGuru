@inject IServiceCatalog Catalog

@{
    var heroPoints = new[] { "Verified professionals", "Multiple quotes", "Easy comparison", "Pan-India network" };

    var slides = new[] { "hero-1", "hero-2", "hero-3", "hero-4" };
}

<section class="relative overflow-hidden bg-hero pt-28 lg:pt-36">

    @* Four photographs crossfading on a 40 second loop, 10 seconds each.
       All the motion lives in app.css, so nothing runs in JavaScript. *@
    <div class="hero-media" aria-hidden="true">
        @for (var i = 0; i < slides.Length; i++)
        {
            <img src="~/images/hero/@(slides[i]).webp"
                 alt=""
                 class="hero-slide object-center lg:object-right"
                 width="1920" height="1080"
                 decoding="async"
                 loading="@(i == 0 ? "eager" : "lazy")"
                 fetchpriority="@(i == 0 ? "high" : "low")" />
        }
    </div>

    @* Main scrim: solid cream on the left, clearing completely by 80% across
       so the right of the photograph is untouched. On phones it washes the
       whole photo instead, because there is no room beside it. *@
    <div class="pointer-events-none absolute inset-0 bg-[linear-gradient(180deg,rgba(254,253,251,0.94)_0%,rgba(254,253,251,0.82)_55%,rgba(254,253,251,0.65)_100%)] lg:bg-[linear-gradient(90deg,rgba(254,253,251,1)_0%,rgba(254,253,251,0.97)_34%,rgba(254,253,251,0.72)_52%,rgba(254,253,251,0)_80%)]" aria-hidden="true"></div>

    @* Soft vignette: pulls the corners down so the photo sits back rather
       than shouting. *@
    <div class="pointer-events-none absolute inset-0 bg-[radial-gradient(ellipse_at_30%_40%,transparent_0%,rgba(24,34,56,0.12)_100%)]" aria-hidden="true"></div>

    @* Band under the navbar, so the menu stays readable over any photo. *@
    <div class="pointer-events-none absolute inset-x-0 top-0 h-28 bg-gradient-to-b from-hero via-hero/70 to-transparent lg:h-32" aria-hidden="true"></div>

    @* The photo dissolves into the page instead of ending on a hard edge. *@
    <div class="pointer-events-none absolute inset-x-0 bottom-0 h-40 bg-gradient-to-t from-hero via-hero/60 to-transparent" aria-hidden="true"></div>

    <div class="relative mx-auto max-w-[1400px] px-5 sm:px-8">

        <div class="grid gap-12 lg:grid-cols-12 lg:gap-10">

            <!-- Message -->
            <div class="lg:col-span-7 lg:pb-28 xl:col-span-7">

                <p class="hero-in hero-in-1 flex items-center gap-3 text-[13px] font-semibold uppercase tracking-[0.14em] text-jade-500">
                    <span class="h-px w-8 bg-jade-500" aria-hidden="true"></span>
                    Your move, better organized
                </p>

                <h1 class="hero-in hero-in-2 mt-7 font-display text-[clamp(2.75rem,8vw,5.5rem)] font-extrabold leading-[0.95] tracking-[-0.045em] text-ink-900">
                    <span class="block">Move without</span>
                    <span class="block">the <span class="text-jade-500">moving stress.</span></span>
                </h1>

                <p class="hero-in hero-in-3 mt-8 max-w-md text-lg leading-relaxed text-ink-700">
                    Compare moving professionals, receive quotes and choose the right
                    option for your move.
                </p>

                <div class="hero-in hero-in-4 mt-9 flex flex-col gap-3 sm:flex-row sm:items-center">
                    <a href="#get-quotes"
                       class="inline-flex h-13 items-center justify-center bg-jade-500 px-7 text-base font-semibold text-white shadow-cta rounded-card transition-colors hover:bg-jade-600 lg:hidden">
                        Get Free Quotes
                    </a>
                    <a href="/how-it-works"
                       class="inline-flex h-13 items-center justify-center border border-ink-300 bg-white px-7 text-base font-semibold text-ink-900 shadow-card rounded-card transition-colors hover:border-jade-500 hover:text-jade-600">
                        How It Works
                    </a>
                </div>

                <ul class="hero-in hero-in-4 mt-12 grid max-w-lg grid-cols-2 gap-x-8 gap-y-5 border-t border-ink-200 pt-8">
                    @foreach (var point in heroPoints)
                    {
                        <li class="flex items-start gap-2.5">
                            <svg class="mt-1 h-3.5 w-3.5 shrink-0 text-success-600" viewBox="0 0 20 20" fill="none"
                                 stroke="currentColor" stroke-width="2.6" aria-hidden="true">
                                <path d="M4 10.5l4 4 8-9" stroke-linecap="round" stroke-linejoin="round" />
                            </svg>
                            <span class="text-sm font-medium text-ink-800">@point</span>
                        </li>
                    }
                </ul>
            </div>

            @* The form card drops below the hero's bottom edge on desktop.
               That overlap is what ties the photograph to the page below it. *@
            <div class="lg:col-span-5 lg:-mb-24 lg:pt-4">
                <div id="get-quotes"
                     class="hero-in hero-in-5 scroll-mt-28 border border-ink-200 bg-white p-6 shadow-float rounded-panel sm:p-8">

                    <div class="flex items-baseline justify-between">
                        <h2 class="font-display text-[22px] font-bold tracking-[-0.03em] text-ink-900">Start your move</h2>
                        <span class="text-xs font-medium text-ink-500">Takes a minute</span>
                    </div>
                    <p class="mt-2 text-[15px] leading-relaxed text-ink-500">
                        Tell us the basics. We'll match you with relevant professionals.
                    </p>

                    @* A GET hand-off, not a post. Whatever is filled in here arrives
                       at /quote as query parameters and pre-fills the first two
                       steps. One conversion path, nothing submitted from here. *@
                    <form action="/quote" method="get" class="mt-7 space-y-5">

                        <div>
                            <label for="hero-service" class="block text-[13px] font-semibold tracking-wide text-ink-800">What service do you need?</label>
                            <select id="hero-service" name="service"
                                    class="mt-2 block h-12 w-full border-0 border-b border-ink-200 bg-transparent px-0 text-[15px] text-ink-900 focus:border-jade-500 focus:ring-0">
                                <option value="">Select a service</option>
                                @foreach (var service in Catalog.GetAll())
                                {
                                    <option value="@service.Slug">@service.Name</option>
                                }
                            </select>
                        </div>

                        <div class="grid gap-5 sm:grid-cols-2">
                            <div>
                                <label for="hero-from" class="block text-[13px] font-semibold tracking-wide text-ink-800">Moving from</label>
                                <input id="hero-from" name="from" type="text" autocomplete="address-level2" placeholder="Gurgaon"
                                       class="mt-2 block h-12 w-full border-0 border-b border-ink-200 bg-transparent px-0 text-[15px] text-ink-900 placeholder:text-ink-400 focus:border-jade-500 focus:ring-0" />
                            </div>

                            <div>
                                <label for="hero-to" class="block text-[13px] font-semibold tracking-wide text-ink-800">Moving to</label>
                                <input id="hero-to" name="to" type="text" autocomplete="address-level2" placeholder="Bangalore"
                                       class="mt-2 block h-12 w-full border-0 border-b border-ink-200 bg-transparent px-0 text-[15px] text-ink-900 placeholder:text-ink-400 focus:border-jade-500 focus:ring-0" />
                            </div>
                        </div>

                        <div>
                            <label for="hero-date" class="block text-[13px] font-semibold tracking-wide text-ink-800">Moving date</label>
                            <input id="hero-date" name="date" type="date" data-min-today="true"
                                   class="mt-2 block h-12 w-full border-0 border-b border-ink-200 bg-transparent px-0 text-[15px] text-ink-900 focus:border-jade-500 focus:ring-0" />
                        </div>

                        <button type="submit"
                                class="group mt-3 flex h-14 w-full items-center justify-center gap-2 bg-jade-500 px-6 text-base font-semibold text-white shadow-cta rounded-card transition-colors hover:bg-jade-600">
                            Get Free Quotes
                            <svg class="h-4 w-4 transition-transform duration-200 group-hover:translate-x-1"
                                 viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
                                <path d="M4 10h11M11 6l4 4-4 4" stroke-linecap="round" stroke-linejoin="round" />
                            </svg>
                        </button>

                        <p class="text-center text-xs text-ink-500">Free to use. No obligation to book.</p>
                    </form>
                </div>
            </div>
        </div>
    </div>
</section>