@{
    var navLinks = new (string Text, string Href)[]
    {
        ("Services", "#services"),
        ("Locations", "/locations"),
        ("How It Works", "#how-it-works"),
        ("About Us", "/about"),
        ("Contact", "/contact")
    };
}

@* Transparent over the dark hero, then condenses to solid paper once
   you scroll past it. One class toggle, no scroll maths. *@
<header id="site-header" class="site-header fixed inset-x-0 top-0 z-50 border-b border-transparent">
    <div class="mx-auto flex h-16 max-w-[1400px] items-center justify-between px-5 sm:px-8 lg:h-20">

        <a href="/" class="group flex items-center gap-3" aria-label="ShiftingGuru home">
            <span class="grid h-9 w-9 place-items-center bg-jade-500 rounded-card" aria-hidden="true">
                <svg class="h-4.5 w-4.5 text-ink-950" viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="2">
                    <path d="M3 15h3.2L10 5l3.8 10H17" stroke-linecap="round" stroke-linejoin="round" />
                </svg>
            </span>
            <span data-header-word class="font-display text-[19px] font-extrabold tracking-[-0.03em] text-white">ShiftingGuru</span>
        </a>

        <nav class="hidden lg:flex lg:items-center lg:gap-9" aria-label="Main">
            @foreach (var link in navLinks)
            {
                <a href="@link.Href" data-header-link
                   class="text-[15px] font-medium text-ink-200 transition-colors hover:text-white">@link.Text</a>
            }
        </nav>

        <div class="hidden lg:flex lg:items-center lg:gap-6">
            <a href="/partner/login" data-header-link class="text-[15px] font-medium text-ink-200 transition-colors hover:text-white">
                Partner Login
            </a>
            <a href="#get-quotes"
               class="inline-flex h-11 items-center bg-jade-500 px-6 text-[15px] font-semibold text-ink-950 rounded-card transition-colors hover:bg-jade-400">
                Get Free Quotes
            </a>
        </div>

        <button id="menu-toggle" type="button"
                class="grid h-11 w-11 place-items-center border border-white/25 rounded-card lg:hidden"
                aria-expanded="false" aria-controls="mobile-drawer" aria-label="Open menu">
            <svg data-header-icon class="h-5 w-5 text-white" viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.8" aria-hidden="true">
                <path d="M3 6h14M3 10h14M3 14h14" stroke-linecap="round" />
            </svg>
        </button>
    </div>
</header>

<div id="mobile-overlay" hidden class="fixed inset-0 z-50 bg-ink-950/60 lg:hidden"></div>

<div id="mobile-drawer" hidden
     class="drawer fixed right-0 top-0 z-50 flex h-dvh w-[86%] max-w-sm flex-col bg-paper lg:hidden"
     role="dialog" aria-modal="true" aria-label="Site menu">

    <div class="flex h-16 items-center justify-between border-b border-ink-100 px-5">
        <span class="font-display text-base font-bold tracking-tight text-ink-900">Menu</span>
        <button id="menu-close" type="button"
                class="grid h-11 w-11 place-items-center border border-ink-100 rounded-card" aria-label="Close menu">
            <svg class="h-5 w-5 text-ink-900" viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.8" aria-hidden="true">
                <path d="M5 5l10 10M15 5L5 15" stroke-linecap="round" />
            </svg>
        </button>
    </div>

    <nav class="flex-1 overflow-y-auto px-5 py-3" aria-label="Mobile">
        <ul class="divide-y divide-ink-100">
            @foreach (var link in navLinks)
            {
                <li>
                    <a href="@link.Href" class="block py-4 font-display text-lg font-semibold tracking-tight text-ink-900">@link.Text</a>
                </li>
            }
        </ul>
    </nav>

    <div class="space-y-3 border-t border-ink-100 p-5">
        <a href="#get-quotes"
           class="block bg-jade-500 py-3.5 text-center text-base font-semibold text-ink-950 rounded-card">
            Get Free Quotes
        </a>
        <a href="/partner/login"
           class="block border border-ink-200 py-3.5 text-center text-base font-medium text-ink-800 rounded-card">
            Partner Login
        </a>
        <a href="/join-as-partner"
           class="block py-2 text-center text-sm font-medium text-ink-500">
            Join as Partner
        </a>
    </div>
</div>
